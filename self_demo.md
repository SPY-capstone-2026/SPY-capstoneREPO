# Moni Self-Demo 가이드

이 문서는 별도 설치 없이 **배포된 웹앱에 접속해 Moni의 핵심 동작을 직접 확인**하기 위한 시연 가이드입니다. 브라우저만 있으면 순서대로 따라 할 수 있습니다.

> 시연 주소: **https://spy-capstone-repo.vercel.app**
> 권장 환경: PC 크롬 브라우저 (시크릿 창 권장 — 이전 로그인 상태의 영향을 받지 않습니다)

---

## 한눈에 보기

| 시나리오 | 목적 | 사용 계정 |
|----------|------|-----------|
| A. 데이터가 있는 사용자 | Prophet 예측 기반 챌린지 생성 확인 | `office@moni.app` |
| B. 데이터가 없는 신규 사용자 | 데이터 부족 시 fallback 챌린지 동작 확인 | 직접 회원가입 |

두 시나리오를 모두 따라 하면, **소비 데이터가 충분할 때(AI 예측)와 없을 때(기본 챌린지)의 동작 차이**를 함께 확인할 수 있습니다.

---

## 시나리오 A — 데이터가 있는 사용자 (Prophet 예측)

1년치 직장인 소비 더미데이터가 들어있는 계정으로, **예측 기반 챌린지가 실제로 생성되는 과정**을 확인합니다.

### 1단계. 로그인

1. 시연 주소(https://spy-capstone-repo.vercel.app) 에 접속합니다.
2. 아래 계정으로 로그인합니다.

   ```text
   아이디(이메일): office@moni.app
   비밀번호: 1234
   ```

### 2단계. 홈 — 예측 결과 확인

로그인 후 홈 화면에서 다음을 확인합니다.

- **이번 달 현재 지출**
- **월말 예상 지출** — 과거 소비 패턴을 바탕으로 Prophet이 예측한 값
- **예산 대비 상태** — 예산 대비 어느 정도 압박을 받고 있는지

> 확인 포인트: 단순 합계가 아니라 **남은 기간의 소비까지 예측한 "월말 예상 지출"**이 표시되는지 봅니다. 이것이 Moni의 핵심인 예측 기반 구조입니다.

### 3단계. 챌린지 — 예측 기반 챌린지 생성

1. 하단 탭에서 **챌린지**로 이동합니다.
2. 오늘의 챌린지가 없다면 **오늘의 챌린지 생성**을 누릅니다.
3. 생성된 챌린지를 확인합니다.

> 확인 포인트: 카테고리별 **예산 압박도에 따라 난이도가 다른 챌린지**가 생성됩니다. 압박도가 높은 카테고리는 금지형(예: "오늘은 카페 지출 없이 보내보세요"), 낮은 카테고리는 유지형 챌린지가 나옵니다. 무지출이 이어진 카테고리에는 streak 보너스 챌린지가 추가될 수 있습니다.

### 4단계. 챌린지 달성 — 성장 구조

1. 챌린지를 **완료** 처리합니다.
2. XP가 오르고 레벨에 반영되는지 확인합니다.
3. **완료 취소**를 누르면 XP가 원래대로 돌아오는지도 확인할 수 있습니다.

### 5단계. 리포트

**리포트** 탭에서 이번 달 지출, 월말 예상 지출, 예산 압박도, 요일별 소비 리듬, 카테고리별 예산 상태를 확인합니다.

---

## 시나리오 B — 데이터가 없는 신규 사용자 (fallback)

소비 기록이 전혀 없는 신규 사용자가 가입했을 때의 동작을 확인합니다. 데이터가 부족하면 예측을 돌릴 수 없으므로, **기본(fallback) 챌린지로 자연스럽게 대체되는지**를 봅니다.

### 1단계. 회원가입

1. 시연 주소에 접속해 **회원가입**을 진행합니다. (시나리오 A와 구분되도록 새 이메일을 사용합니다.)
2. 가입 시 수입 유형 / 수입일 / 소비 성향을 입력합니다.

### 2단계. 빈 상태 확인

로그인 후 홈 / 소비 / 리포트 화면에서 **지출 데이터가 없을 때의 안내 UI**가 표시되는지 확인합니다.

> 확인 포인트: 데이터가 없어도 앱이 오류 없이 빈 상태를 안내하는지 봅니다.

### 3단계. fallback 챌린지

1. **챌린지** 탭에서 오늘의 챌린지를 생성합니다.
2. 소비 데이터가 없어도 기본 챌린지가 생성되는지 확인합니다.

> 확인 포인트: 데이터가 부족할 때는 예측 대신 **기본 챌린지가 제공**됩니다. 사용자 화면에는 "fallback" 같은 개발자 용어 없이 자연스러운 챌린지로 표시됩니다.

### (선택) 4단계. 데이터를 넣었을 때의 변화

신규 계정에서 **소비** 탭으로 이동해 지출을 며칠치 직접 추가한 뒤 다시 챌린지를 생성하면, 데이터가 쌓일수록 예측·챌린지가 어떻게 달라지는지 비교해볼 수 있습니다.

---

## 참고

- 이 가이드는 **배포된 웹앱 기준**입니다. 소스 코드를 내려받아 로컬에서 직접 실행하는 방법은 루트 `README.md`의 "실행 방법"을 참고하세요.
- 시연 계정 `office@moni.app`에는 1년치 직장인 소비 패턴 더미데이터가 미리 들어가 있습니다.
- 시연 중 화면이 비어 보이면, 백엔드 서버가 절전 상태에서 깨어나는 중일 수 있습니다. 잠시 후 새로고침해 주세요.
